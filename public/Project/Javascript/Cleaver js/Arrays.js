//Arrays
//Arrays allow you to store multiple values inside of them
const groceries=['banna','apple','orange']
console.log(groceries)
console.log(groceries[1])
//array method (slice,push,indexof,length)
//The index gives you the location of a value within an array.It is similar to a street number
groceries.push("Mangoo")
console.log(groceries)
//Adds one or more elements to the end of an array and returns the  new length of the array.
console.log(groceries.slice(0,2))
//The slice() method return the portion of the array you want in a new array.
console.log(groceries.indexOf('apple'))


console.log(groceries.length)
//Length is an array property that return the number of elements you have in a given array
